// material-ui
import { CircularProgress } from '@mui/material';

//===========================|| SPINNER ||===========================//

export const Spinner = (props) => <CircularProgress {...props} />;
